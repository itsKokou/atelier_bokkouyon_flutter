import 'package:flutter/material.dart';

class SearchOnMapPage extends StatelessWidget {
  const SearchOnMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(), 
      body: SafeArea(
        child: Text(""),
      ),
    );
  }
}
